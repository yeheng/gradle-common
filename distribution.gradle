apply plugin: 'distribution'

distributions {
	distTar.enabled = false
    main {
        contents {
            from { 'src/assemble' }
            from (project.buildDir.path) {
              include "libs/*.jar"
            }
        }
    }
}
